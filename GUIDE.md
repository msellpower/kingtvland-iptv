# 👑 KINGTVLAND - מדריך מערכת מלא, העלאה, אינטגרציות ואפיון (Comprehensive Guide)

ברוכים הבאים למדריך המלא עבור מערכת **KINGTVLAND**. מדריך זה מרכז את כל המידע הטכני, איפיון המערכת, תהליכי הפריסה (Deployment) לProduction ב-Netlify, והאינטגרציות עם שירותי צד שלישי (Google Apps Script, Firebase, Telegram, TMDB, Gemini AI, Crypto & PayPal).

---

## 📋 תוכן עניינים
1. [אפיון המערכת ויכולות (Features)](#אפיון-המערכת-ויכולות-features)
2. [ארכיטקטורה וטכנולוגיות](#ארכיטקטורה-וטכנולוגיות)
3. [מדריך פריסה והתקנה ל-Netlify](#מדריך-פריסה-והתקנה-ל-netlify)
4. [הגדרת מסד נתונים - Google Apps Script](#הגדרת-מסד-נתונים---google-apps-script)
5. [הגדרות אינטגרציות ו-API](#הגדרות-אינטגרציות-ו-api)
    - [Telegram Bot](#telegram-bot)
    - [Gemini AI (Chatbot)](#gemini-ai-chatbot)
    - [TMDB (סרטים וסדרות)](#tmdb-סרטים-וסדרות)
    - [Firebase](#firebase)
    - [Cryptocurrency (Ramp) & PayPal](#cryptocurrency-ramp--paypal)

---

## 1. אפיון המערכת ויכולות (Features)

**KINGTVLAND** היא פלטפורמת פרימיום למכירת מנויי סטרימינג / IPTV, הכוללת:
* **חזית (Frontend):** פלטפורמה מודרנית ונקייה, התומכת בעברית ובאנגלית, עם מצב צפייה בסרטים, סדרות וערוצים לייב. היררכיה ברורה של תוכניות פרימיום, תכני מבוגרים מוגנים בסיסמה, המלצות אפליקציות צפייה (TiviMate, Smarters) ותמיכה בטלוויזיות/סטרימרים ומובייל.
* **מערכת תמיכה ושירות:** 
  - צ'אט בוט חכם (מבוסס רשת נוירונים / Gemini AI) לתמיכה טכנית בסיסית.
  - כפתור WhatsApp ו-Telegram לקישור ישיר לנציג.
  - שליחת טופס דיווח תקלות.
* **מערכת תשלומים (Billing):**
  - תמיכה בתשלומים בכרטיס אשראי, PayPal ומטבעות קריפטוגרפיים (דרך Ramp Network וסליקה ייעודית).
* **מערכת שותפים/משווקים (Reseller Program):** פאנל למשווקים שמנהלים מנויים ויכולים לנהל עסקאות בצורה עצמאית (CRM מובנה).
* **פאנל ניהול (Admin Dashboard):**
  - עדכון סטטוסים בזמן אמת, צפייה בהזמנות, משתמשים ורווחים.
  - שליטה על פופאפים של מבצעים ללקוחות.
  - שליחת התראות (Push / Telegram) למנויים ולמנהל.
* **בלוג וחדשות (Blog/News):** התממשקות למערכות RSS ול-TMDB בשביל הצגת המלצות צפייה עדכניות.

---

## 2. ארכיטקטורה וטכנולוגיות

* **Frontend:** React 18, TypeScript, Vite, Tailwind CSS, Recharts (סטטיסטיקות וממשק ניהול).
* **Backend Proxy:** Node.js, Express `server.ts` משרת כ-Proxy להסתרת מפתחות API וסליקת נתונים אלטרנטיבית. מנוע Vite Middleware רץ בסביבת פיתוח ו-Express בפרודקשיין (מומר ל-`dist/server.cjs` דרך Webpack/Esbuild).
* **מסד נתונים ראשי (Headless DB):** Google Apps Script - פועל כגשר לבסיס הנתונים שיושב על Google Sheets, עקיף מגבלות שרת/עלויות ונותן למערכת לעבוד באופן חינמי.
* **מסד נתונים גיבוי / העדפות אישיות:** Firebase Firestore לסינכרון העדפות משתמש במקרי הצורך.
* **Hosting:** מותאם להעלאה ל-[Netlify](https://www.netlify.com/) תחת `netlify/functions` ברובד חינמי מלא.

---

## 3. מדריך פריסה והתקנה ל-Netlify

המערכת מקונפגת במדויק בחוזה ה-`netlify.toml` לעבוד באמצעות Netlify Functions על מנת שתספק שירותי API (הסתרת סודות) לצד שרת.

1. **הכנת קוד המקור:**
   וודאו שכל הקבצים מגובים והועלו ל-**GitHub** (המאגר).
   
2. **התחברות ל-Netlify:**
   - היכנסו ל-Netlify וצרו "New site from Git".
   - בחרו את הפרויקט הרלוונטי מה-GitHub שלכם.

3. **הגדרות בנייה (Build Settings):**
   - Directory: `.`
   - Build command: `npm run build`
   - Publish directory: `dist`
   
4. **מפתחות ומשתני סביבה ב-Netlify (Environment Variables):**
   גשו ל-**Site settings** > **Environment variables** והוסיפו:
   * `GOOGLE_SCRIPT_URL` - כתובת ה-Web App שתפיקו מ-Apps Script (ראו שלב 4).
   * `GEMINI_API_KEY` - מפתח ה-API של גוגל מ-AI Studio עבור כלי הצ'אט בוט. (חשוב למנוע את הבוט המלאכותי).
   * `TELEGRAM_BOT_TOKEN` - לצורך התראות למנהל ולערוצים (אופציונלי).
   * `ADMIN_CHAT_ID` - מספר זיהוי הטלגרם לאן יישלחו התראות ודיווחים מהאפליקציה.
   
5. **פרסום (Deploy):**
   לחצו על "Deploy Site". ברגע שזה מסיים המערכת באוויר באופן פונקציונלי ומאובטח.

---

## 4. הגדרת מסד נתונים - Google Apps Script

ללא שרת DB יקר, המערכת משתמשת בגוגל שיטס מאובטח כ-DB חזק וחינמי (נוצר אוטומטית מקובץ הקוד `code.txt`).

1. **יצירת פרויקט במסד הנתונים:**
   - גשו אל [Google Apps Script](https://script.google.com/home), ולחצו על "New Project".
   - פתחו עמוד חדש של Google Sheets והעתיקו את ה**ID** של הקובץ מה-URL שלו (מה שבין ה `/d/` ולפני ה `/edit`).
   - העתיקו את **כל התוכן** מהקובץ `code.txt` והדביקו בעורך ה-Apps Script.
   - בתוך הקוד, שנו את `SPREADSHEET_ID` ל-ID של גיליון ה-Sheets החדש שלכם או שנעשה את זה אוטומטית בהרצת `setup()`.

2. **הפעלת הרצה ראשונית למסד הנתונים:**
   - בחרו את הפונקציה `setup` מתפריט הריצה למעלה (Run).
   - לחצו על Run (הרץ). גוגל תבקש אישור הרשאות -> אשרו (Advanced -> Go to unsafe).
   - פעולה זו **תבנה אוטומטית** את כל 13+ הגליונות הדרושים למערכת (Orders, Customers, Settings, Tickets וכד').

3. **פריסה לרשת (Deploy):**
   - לחצו (בפינה הימנית למעלה) **Deploy > New deployment**.
   - סוג (Type): בחרו בגלגל שיניים **Web app**.
   - תיאור: v1.
   - Execute as: **Me** (האמייל שלכם).
   - Who has access: **Anyone** (הכרחי למערכות ה-API שיגשו משם).
   - העתיקו את ה-**Web App URL**, הדביקו אותו למערכת ב-Netlify תחת `GOOGLE_SCRIPT_URL` ואל קובץ ה `.env` שלכם בפיתוח לוקאלי.

---

## 5. הגדרות אינטגרציות ו-API

כדי שהמערכת תתפקד "כמכונה משומנת" אוטומטית ללא מגע יד אדם יש לבצע את הקישורים הבאים:

### Telegram Bot
לצורך משיכת התראות, הזמנות חדשות, בקשות תמיכה ותקלות היישר למכשיר שלכם.
* צרו בוט חדש מול `@BotFather` בטלגרם.
* קחו את ה `TELEGRAM_BOT_TOKEN` שמקבלים.
* השיגו את ה `ADMIN_CHAT_ID` מול ראיית ה-ID שלכם בטלגרם (למשל מבוט מציאת IDs).
* הגדירו אותם כמשתני סביבה (Environment variables ב־Netlify או .env מקומי).

### Gemini AI (Chatbot)
סייען תמיכה טכנית משולב באפליקציה (ChatBot.tsx המשתמש ב `server.ts` לשליפת API).
* קחו מפתח מ-[Google AI Studio](https://aistudio.google.com).
* הזינו אותו בתוך משתנה הסביבה `GEMINI_API_KEY`. מנגנון הפרוקסי הקיים (api/public/chat/ai) ידאג לא לחשוף אותו לפרונט לעולם.

### TMDB (סרטים וסדרות)
לאיבוד ועיבוד תצוגות (ממשק הבא מ- `newsService.ts` במקביל למשיכת API ב-server).
* גשו לאתר TMDB > הגדרות > API והפיקו מפתח API (רשום מפתח כבר בקוד ב `server.ts` לצורך פיתוח, אך כדאי להגדיר בעתיד ב `.env` אמיתי באותה השיטה של Gemini).

### Firebase
המערכת מכילה ארכיטקטורה כפולה, ומאפשרת לשמור העתקי העדפות עולמיים דרך Firebase (DB_MODE דינאמי).
הפרויקט קונפג דרך הצימוד המקורי ב- `firebase-applet-config.json` וחוקי האבטחה (Firestore rules). אם רוכבים אך ורק על Google השימוש הוא כאופציה חילופית בלבד.

### Cryptocurrency (Ramp) & PayPal
* **Ramp Network**: מנגנון הקנייה בקריפטו נטען אופציונלית באפיון ומאפשר סליקת תשלום USDT (רשת Tron). במערכת הלקוח מקבל כתובת מדויקת שמוגדרת לפי הארנק המרכזי של החברה. עריכת הכתובת לארנק נעשית בקובץ `constants.ts` תחת `CRYPTO_CONFIG`.
* **PayPal**: מוטמעת האופציה לתשלום מהיר ללקוח לפייפאל אישי (`PAYPAL_EMAIL` ערוך בקבצי האפליקציה / Admin panel).
  
---

## 🔒 אבטחת מידע בפרויקט (Security Protocols)
1. **הסתרת סודות:** כל מפתחות ה-AI, מפתחות TMDB ו-Telegram מעובדים בצד שרת בלבד. הלקוח שולח בקשות אל `api/xxx` ובכך מוסתרים.
2. **סיסמאות והצפנה:** רישום התחברות למנהל מחייבת סיסמה (ברירת המחדל האוטומטית שתיבנה בSheets: `2202`). האפליקציה דוחפת Tokens לעוגיות מאובטחות (Secure flag מקודד). אימות 2-Factor Authentication קיימת במודל המערכת.
3. **CORS Headers:** במידת הוספת תכונות, הפעלת CORS מסודר בקובץ `server.ts` ללקוחות ייעודיים בלבד למניעת ספאמים על תקציב הדאטה של Netlify/Google.

---

**תהנו מסביבת המכירות החדשה שלכם! 🎉**

לכל בעיה או חקירה טכנית נא לפנות חזרה למהנדסי ה-AI במערכת KINGTVLAND.
