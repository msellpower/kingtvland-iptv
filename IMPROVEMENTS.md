# דוח שיפורים והמלצות למערכת KINGTVLAND

מסמך זה מרכז המלצות לשיפור המערכת, תוך שמירה על יציבות הקוד הקיים. ההמלצות מחולקות לפי קטגוריות: אבטחה, ביצועים, חווית משתמש (UX) ותחזוקה.

## ✅ שיפורים שבוצעו לאחרונה (מאי 2026)

### 🛡️ אבטחה: הסרת Legacy Tokens
בוצע מעבר מלא לאימות מבוסס Firebase Claims בלבד ב-`server.ts`. הוסרו טוקנים מיושנים כמו `admin_active` כדי למנוע אפשרות לעקיפת אבטחה.

### 🚀 ביצועים: ניהול Caching עם React Query
הותקנה והוגדרה ספריית `@tanstack/react-query` ב-`App.tsx`. המערכת כעת מוכנה למעבר הדרגתי של כל הקריאות ל-API לניהול מטמון חכם, מה שיפחית משמעותית את העומס על השרת וישפר את מהירות התגובה למשתמש.

### ⚡ ביצועים: Lazy Loading לתמונות
נוספה תכונת `loading="lazy"` למרבית התמונות באתר (כולל באדג'ים של חנויות אפליקציות) לשיפור מהירות הטעינה הראשונית והורדת צריכת הנתונים. עמודי ה-Hero הוגדרו עם `loading="eager"` ו-`fetchPriority="high"` לביצועים אופטימליים.

### 🤖 חווית משתמש: מנגנון ריטריי (Retry) בצאט
הוסף מנגנון "Retry" אוטומטי (3 ניסיונות עם השהיה) לקריאות ה-API של Gemini בצאטבוט, לטיפול במקרים של עומס רגעי על השרתים.

---

## 🚀 המלצות לשיפורים עתידיים

## 1. אבטחה (Security) 🔒

### סניטציה של תוכן HTML (קריטי)
בכרטיסיות הבלוג ובמודל הפרטים, אנו משתמשים ב-`dangerouslySetInnerHTML` כדי להציג את תוכן הכתבות שנמשך מאתרים חיצוניים.
*   **הסיכון:** אם האתר החיצוני נפרץ או מכיל קוד זדוני (XSS), הוא עלול לרוץ בדפדפן של המשתמשים שלך.
*   **ההמלצה:** שימוש בספרייה כמו `dompurify` כדי לנקות את ה-HTML לפני ההצגה. (כבר קיימת גרסה ראשונית במיקומי מפתח, יש להרחיב לכל רכיבי ה-Custom Content).

### הגנת Rate Limiting מבוססת Redis/DB
כרטיסי ה-Rate Limit הנוכחיים הם בזיכרון השרת (In-memory).
*   **הסיכון:** באיפוס של השרת (או בשימוש במיכלים מרובים), המגבלות מתאפסות.
*   **ההמלצה:** שימוש ב-Store חיצוני (כמו Firestore או Redis) לניהול ה-Rate Limit כדי להבטיח אכיפה עקבית.

## 2. ביצועים (Performance) 🚀

### העברת useEffect ל-React Query
*   **המשימה:** להחליף את הקריאות ב-`AppsGuide`, `AdminDashboard` ו-`Profile` ב-`useQuery`.
*   **הרווח:** ביטול כפילות קריאות בטעינת עמודים, וניהול אוטומטי של "Loading State" ו-"Error State".

### וירטואליזציה (Virtualization)
אם רשימת הסרטים/כתבות תגדל למאות פריטים.
*   **ההמלצה:** שימוש ב-`react-window` כדי לרנדר רק את הפריטים שנמצאים כרגע במסך, מה שישפר משמעותית את ביצועי הגלילה במובייל.

## 3. יציבות ותחזוקה (Backend - Google Apps Script) 🛠️

### ניטור שגיאות סריקה (Scraping Health Check)
הפונקציה `handleGetArticleDetails` מסתמכת על מבנה ה-HTML של אתר חיצוני. אם האתר ישנה עיצוב, הקוד יישבר.
*   **ההמלצה:** יצירת טריגר יומי ב-Apps Script שמנסה לסרוק כתבה לדוגמה. אם הסריקה נכשלת (לא מוצאת כותרת/תוכן), המערכת תשלח התראה לטלגרם של המנהל.

## 4. חווית משתמש (UX/UI) 🎨

### חיפוש וסינון מתקדמים
כיום החיפוש הוא טקסטואלי בלבד.
*   **ההמלצה:** הוספת פילטרים לפי ז'אנר, דירוג ושנת יציאה (נתונים שקיימים ב-TMDB).

---
**הערה:** כל השיפורים הנ"ל ניתנים ליישום בהדרגה, כתוספות לקוד הקיים, ללא צורך בשכתוב המערכת.
